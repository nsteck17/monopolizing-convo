package stechschulte.uno.ese.monopolizing_convo.file.util;

import stechschulte.uno.ese.monopolizing_convo.vo.MonopolizedConvoVO;
import java.util.List;

public class LogicalVerification {

    public boolean checkForLogicalVerification(List<MonopolizedConvoVO> monoConvoList) {

        boolean isValid = true;

        // is there group size set correct

        // is the user set have the correct number of people

        // is the user set do not have duplicates

        // is the message list sorted by time

        // is the sender of each message in the user set

        return isValid;
    }

}
